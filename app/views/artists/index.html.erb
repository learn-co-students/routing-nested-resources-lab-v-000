<% if session.has_key?(:alert_message) %>
  <%= session[:alert_message] %>
  <%session.delete(:alert_message)%>
<% end %>

<h1>Artists</h1>
<ul>
  <% @artists.each do |artist| %>
    <li><%= link_to artist.name, artist_songs_path(artist) %></li>
  <% end %>
</ul>
