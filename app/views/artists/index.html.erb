<h1>Artists</h1>
<ul>
  <% @artists.each do |artist| %>
    <!-- use the new nested resource route URL helper to link to the index of all songs by that artist. -->
    <li><%= link_to artist.name, artist_songs_path(artist) %></li>
  <% end %>
</ul>
