<h1>Artists</h1>
<ul>
  <% @artists.each do |artist| %>
    <li><%= link_to artist.name, artist_path(artist) %></li>
    <ul><li><%= link_to artist.name, artist_songs_path(artist) %></li>
    </ul>
    <% end %>
</ul>
