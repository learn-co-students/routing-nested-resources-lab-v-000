<h1>Artists</h1>
<ul>
  <% @artists.each do |artist| %>
    <li><%= link_to artist.name, artist_path(artist) %></li>
    <%= link_to artist.name + "'s songs", artist_songs_path(artist) %>
  <% end %>
</ul>
