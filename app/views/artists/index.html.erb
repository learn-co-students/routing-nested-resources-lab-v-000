<% if flash.include?(:message) %>
  <%= flash[:message] %>
<% end %>

<h1>Artists</h1>
<ul>
  <% @artists.each do |artist| %>
    <li><%= link_to artist.name, artist_songs_path(artist) %></li> <!-- link to all songs within artist -->
  <% end %>
</ul>
