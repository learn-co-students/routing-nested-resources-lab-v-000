<h1><%= @artist.name %></h1>
<ul>
  <% @artist.songs.each do |song| %>
    <li><%= link_to song.title, artist_song_path(@artist, song) %></li><br>
  <% end %>
</ul>

<%= link_to "All Artists", artists_path %>
