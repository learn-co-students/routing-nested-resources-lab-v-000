<h1><%= @artist.name %></h1>
<ul>
  <% @artist.songs.each do |song| %>
    <li><%= song.title %></li>
    <li><%= link_to song.title, artist_song_path(@artist, song) %>
    <li><%= link_to "Add Artist", edit_song_path(song) %>
  <% end %>
</ul>
