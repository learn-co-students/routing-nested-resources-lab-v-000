<h1><%= @artist.name %></h1>
<ul>
  <% binding.pry %>

  <% @artist.songs.each do |song| %>
  <% binding.pry %>
  
    <li><%= link_to @song.title, artist_songs_path(@artist, @song) %></li>
  <% end %>
</ul>
