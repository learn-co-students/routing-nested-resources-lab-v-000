<!DOCTYPE html>
<html>
  <head>
    <title>Flatiron Tunes</title>
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <% flash.each do |name, msg| -%>
      <%= content_tag :div, msg, class: name %>

    <!-- This original code seems to have an error in it -->
    <!-- deleted code -->
    <!-- <#% end -%> -->

    <!-- new code -->
    <% end %>

    <%= yield %>
  </body>
</html>
