<ul>
<% @songs.each do |song| %>
  <li><%= display_artist(song) %> - <%= link_to @artist.song.title, artist_songs_path(@artist) %></li>
<% end %>
</ul>
<br>
<%= link_to "Add New Song", new_song_path %>
