<ul>

<% @songs.each do |song| %>

    <li><%= display_artist(song) %> - <%= link_to song.title, song_path(song) %></li>

<% end %>
</ul>
<br>
<%= link_to "Add New Song", new_song_path %>

<!-- this is artist_songs_path via songs#index -->
<!--It makes no sense that @song is a variabe here other than to pass tests -->
