<ul>
<% @artist.songs.each do |song| %>

  <li><%= display_artist(song) %> - <%= link_to song.title, artist_song_path(@artist, song) %></li>
  # 2 arguments it uses the id for both the nested resources.

    <li><%= link_to @artist.name, artist_songs_path(@artist) %></li>

<% end %>
</ul>
<br>
<%= link_to "Add New Song", new_song_path %>
