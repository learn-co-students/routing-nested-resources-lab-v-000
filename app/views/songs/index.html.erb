<ul>
<% @songs.each do |song| %>
  <li><%= display_artist(song) %> - <%= link_to song.title, song_path(song) %></li>
  <%= link_to "Add New Song", edit_song_path(song) if song.artist.blank? %>
<% end %>
</ul>
<br>
